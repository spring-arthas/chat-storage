# 正式发包说明

## 目标

生成可发给其他 macOS 同事直接安装运行的正式 DMG。

这套流程包含：

- `Developer ID Application` 签名
- Hardened Runtime
- Apple notarization
- stapler 回写公证票据

仅仅用 `CODE_SIGN_IDENTITY="-"` 打出来的 ad-hoc 包，不适合跨机器分发。

## 前置条件

### 1. 本机钥匙串里有正式签名证书

需要安装：

- `Developer ID Application: <你的公司或个人名称> (<TEAM_ID>)`

可用下面命令检查：

```bash
security find-identity -v -p codesigning
```

### 2. 已配置 notarytool 凭据

推荐先在本机创建一个 keychain profile：

```bash
xcrun notarytool store-credentials "chat-storage-notary" \
  --apple-id "<APPLE_ID>" \
  --team-id "<TEAM_ID>" \
  --password "<APP_SPECIFIC_PASSWORD>"
```

## 发包脚本

项目已提供脚本：

`chat-storage/scripts/build_release_dmg.sh`

使用方式：

```bash
DEVELOPER_TEAM_ID="<TEAM_ID>" \
DEVELOPER_ID_APPLICATION="Developer ID Application: <Name> (<TEAM_ID>)" \
NOTARY_KEYCHAIN_PROFILE="chat-storage-notary" \
bash chat-storage/scripts/build_release_dmg.sh
```

生成产物：

- `chat-storage.dmg`

默认会执行：

1. Release Archive
2. 对 `.app` 做正式签名校验
3. 创建 `.dmg`
4. 对 `.dmg` 签名
5. 提交 notarization
6. `stapler staple`

## 仅签名不公证

如果只是临时内部验证签名链，可以跳过公证：

```bash
DEVELOPER_TEAM_ID="<TEAM_ID>" \
DEVELOPER_ID_APPLICATION="Developer ID Application: <Name> (<TEAM_ID>)" \
SKIP_NOTARIZATION=1 \
bash chat-storage/scripts/build_release_dmg.sh
```

但这种包不建议发给最终同事使用。

## 验证命令

### 检查 app 签名

```bash
codesign -dv --verbose=4 build/release/chat-storage.xcarchive/Products/Applications/chat-storage.app
```

### 检查 DMG 的 Gatekeeper 状态

```bash
spctl -a -vv -t open chat-storage.dmg
```

## 说明

- 当前工程的 Release 发包流程已经按正式分发场景准备好了。
- 如果本机没有 `Developer ID Application` 证书，脚本会直接失败并提示。
- 如果没有 notary profile，也不会误生成一个“看起来像正式包但其实不能安全分发”的产物。
